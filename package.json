{
  "name": "lna-client",
  "version": "0.1.0",
  "private": true,
  "description": "Local Network Access tester page",
  "scripts": {
    "start": "live-server --port=3001 --host=localhost --entry-file=index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
